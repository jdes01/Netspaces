import { SpaceDTO } from '@netspaces/contracts';
import { BookingCart } from '.';

export default {
  title: 'Components/BookingCart',
  component: BookingCart,
};

export const BookingCartComponent = () => <BookingCart />;
